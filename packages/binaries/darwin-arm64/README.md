# @xfoil/darwin-arm64

Platform package for the headless XFOIL executable used by `xfoil` on
macOS arm64.

This package contains a headless XFOIL 6.99 executable built by
`scripts/build-xfoil-binary.mjs`. It is GPL-licensed because it redistributes
XFOIL.
