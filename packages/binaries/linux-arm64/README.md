# @xfoil/linux-arm64

Platform package for the headless XFOIL executable used by `xfoil` on
Linux arm64.

The `xfoil` executable is staged here by the binary build workflow before a
release. This package is GPL-licensed because it redistributes XFOIL.
